version: '2'
services:
  db:
    image: postgres:11.3-alpine
  memcached:
    image: memcached:alpine
  web:
    image: it52/rails:latest
    command: bin/bundle exec foreman start
    ports:
      - '3000:3000'
    links:
      - db
      - memcached
    volumes:
      - .:/it52
